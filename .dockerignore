# Smart exclusions - keep module structure but exclude large content

# ===== BIGGEST SPACE HOGS =====
# Python virtual environments (multiple copies, ~6GB total)
.mage_ai_venv/
**/venv/
**/*venv*/
**/MLPythonCode/venv/

# GUI client assets (images, sounds, etc.) but keep structure
Mage.Client/release/backgrounds/
Mage.Client/release/sample-decks/
Mage.Client/sounds/
Mage.Client/splash.jpg

# Large test data but keep structure
Mage.Tests/src/test/data/
Mage.Tests/piperDecks/
Mage.Tests/*.dck
Mage.Tests/*.txt

# Development files
.git/
.idea/
.vscode/
*.log
*.tmp
*.temp

# Build artifacts that regenerate
target/
*.class
*.pyc
*.pyo

# Large binary files (but keep JARs for dependencies)
*.zip
*.tar.gz
*.db
*.wav
*.jpg
*.png

# Exclude specific large JAR patterns but keep essential ones
# !**/*.jar (keep all JARs for now)

# Documentation
*.md
README*

# Local development
local-training/
shared-data/
models/

# But KEEP essential structure:
# - All pom.xml files
# - All src/ directories  
# - All Java source files
# - Module directories